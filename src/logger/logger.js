const endpoint = "https://www.functionup.org"
const batch = "radon"

const demo = function() {
    console.log("welcome to my application" + " I am arpit and a part of FunctionUp radon batch    ")
}

module.exports.endpoint = endpoint
module.exports.batch = batch
module.exports.demo = demo
// module.exports = {endpoint,batch,demo}